# Electron Forge's packager and @electron/rebuild expect a flat node_modules
# layout. pnpm's default symlinked layout can break native-module rebuilds and
# asar packaging, so use a hoisted node_modules instead.
node-linker=hoisted
