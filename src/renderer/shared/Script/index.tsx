import { Box, Card, Divider, Drawer, Icon, List, ListItem, ListItemButton, ListItemIcon } from "@mui/material";
import React, { ReactNode } from "react";
import { Link } from "react-router-dom";

type ScriptProps = {
  name: string;
  description: string;
};

const Script = (props: ScriptProps) => {
  return (
    <Card>
    </Card>
  );
}

export default Script;