import { Box, Button, Icon, InputAdornment, Paper, TextField, Typography } from "@mui/material";
import React from "react";

const Dashboard = (): JSX.Element => {
  return (
    <>
      <Typography variant="h5">
        Dashboard
      </Typography>
    </>
  )
};

export default Dashboard;
